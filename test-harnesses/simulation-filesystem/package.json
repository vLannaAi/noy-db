{
  "name": "@noydb/test-simulation-filesystem",
  "version": "0.0.0",
  "private": true,
  "description": "File adapter edge cases (USB, permissions, corrupt)",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@noydb/core": "workspace:*"
  }
}
