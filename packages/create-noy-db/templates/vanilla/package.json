{
  "name": "{{PROJECT_NAME}}",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify": "noy-db verify"
  },
  "dependencies": {
    "@noy-db/hub": "^{{NOYDB_VERSION}}",
    "@noy-db/to-browser-idb": "^{{NOYDB_VERSION}}"
  },
  "devDependencies": {
    "create-noy-db": "^{{NOYDB_VERSION}}",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
