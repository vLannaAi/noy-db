{
  "//": "Ratchet: typechecks runtime test files (*.test.ts) so they can't silently drift from the public API (closes the gap that hid #504's Sealed<V> handle types). `noImplicitAny:false` drops the implicit-any param tax (noise); strictNullChecks/exactOptionalPropertyTypes stay ON. The `exclude` list is the burn-down backlog of files that currently fail typecheck (or import sibling @noy-db/* packages not built in the typecheck CI job) — fix a file's errors (and add any sibling it needs as a hub devDependency), then remove it from this list. New/untouched, non-sibling test files MUST typecheck. Run via `pnpm --filter @noy-db/hub typecheck`.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "../..",
    "noEmit": true,
    "types": [
      "node"
    ],
    "noImplicitAny": false
  },
  "include": [
    "src",
    "__tests__/**/*.test.ts"
  ],
  "exclude": [
    "__tests__/coordinated-cutover-integration.test.ts",
    "__tests__/managed-mode-adoption.test.ts",
    "__tests__/managed-mode-strong-recovery.test.ts",
    "__tests__/managed-passphrase.test.ts",
    "__tests__/money/mv-and-live.test.ts",
    "__tests__/no-self-provision.test.ts",
    "__tests__/schema-introspection.test.ts",
    "__tests__/schema-update-integration.test.ts",
    "__tests__/schema-update/apply-cutover.test.ts",
    "__tests__/shamir-recovery.test.ts",
    "__tests__/snapshots.test.ts",
    "__tests__/transaction.test.ts",
    "__tests__/write-hooks-integration.test.ts"
  ]
}
