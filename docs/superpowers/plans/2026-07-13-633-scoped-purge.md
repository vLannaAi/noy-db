# #633 Hybrid opt-in scoped purge Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close #633 (milestone #27) with the user-ratified HYBRID design: the vault-level UNCONDITIONAL sealed-CEK + blob purges during `forget()` remain the DEFAULT (byte-identical behavior); a new opt-in per-vault knob scopes those purges to via-declaration-backed collections, reporting skipped-but-detected residue through the #648 Residue channel instead of silently narrowing the erasure promise.

**Architecture:** Ground truth is `.superpowers/sdd/scoped-purge-seam-map.md` — every call site, line number, and excerpt. The knob rides the forget strategy config (per-vault by construction, since `forgetStrategy` threads into each `Vault`); the scope predicate and partitioning logic live in `with-audit/forget/**` (mirroring the `_onViaErase`/`forgetDerivedFanout` delegation pattern) so `kernel/vault.ts` — at its EXACT 3939 ceiling — gains only minimal guard lines, each offset in-file.

**Tech Stack:** TypeScript ESM, vitest, packages/hub.

## Global Constraints

- **Ceiling:** `kernel/vault.ts` ≤ 3939 (metric = `split('\n').length`), currently EXACT zero slack. Every net line added must be offset by genuine in-file compression. Scoped logic lives in `with-audit/forget/` — vault.ts changes are guard-line-level only. `collection.ts` ≤ 4472 (also exact), `noydb.ts` ≤ 2385.
- **Behavior lock:** with the knob ABSENT or false, the full existing forget/purge suite passes UNCHANGED — the seam map §5 lists the pinning tests (`forget-sealed-erasure.test.ts`, `per-blob-cek.test.ts`, and the forget/residue suites); do not touch their assertions.
- **Candor law:** scoping is a necessary-but-not-sufficient proxy — `sealRecordToHost()` and `.blob(id)` work WITHOUT declarations (the seam map cites the live tests proving it). Scoped mode must therefore NEVER silently skip: detected residue in undeclared collections is reported through the Residue channel.
- TDD; no Claude attribution; changeset LOCAL only (never committed); conventional commits ending `(#633)`.

---

### Task 1: the `scopedPurge` knob + scope partitioning + residue reporting

**Seam map:** all sections of `.superpowers/sdd/scoped-purge-seam-map.md`.

**Files:**
- Modify: `packages/hub/src/with-audit/forget/strategy.ts` (the strategy/config surface — add the opt-in flag, exact site per seam map §4)
- Modify: `packages/hub/src/with-audit/forget/active.ts` (factory threading)
- Create: `packages/hub/src/with-audit/forget/purge-scope.ts` (the scope predicate + partitioning: which collections are declaration-backed for sealed-CEK [classified/sensitive via bindings] and for blobs [`blobFieldsRegistry`]; the partition of a vault-wide `SEALED_CEK_NS` listing into purge-vs-report sets)
- Modify: `packages/hub/src/kernel/vault.ts` — the two purge sites gain a scoped branch that delegates to `purge-scope.ts` (guard-line-level; offsets mandatory; the blob purge's existing `else` residue-detection branch is the ready-made template per seam map's surprise finding)
- Test: `packages/hub/__tests__/forget/scoped-purge.test.ts` (new)

**Interfaces:**
- Produces on the forget strategy config: `scopedPurge?: boolean` (default absent/false = unconditional, today's behavior).
- Produces in `purge-scope.ts`: a pure partition function consumed by the vault purge sites; residue notices emitted through the EXISTING #648 Residue channel shape (seam map §3) — new residue reason strings for "skipped-undeclared-sealed-cek" and "skipped-undeclared-blob-scan" classes (exact naming: follow the channel's existing reason-string convention).

- [ ] **Step 1: failing tests.** In `scoped-purge.test.ts`: (a) DEFAULT parity — a vault with an undeclared-but-using collection (bare-`sensitive`+`sealRecordToHost`, per the `forget-sealed-erasure.test.ts:112` recipe) forgets a subject → sealed-CEK entries purged vault-wide exactly as today (assert against the same store-level evidence the existing tests use); (b) SCOPED mode — same fixture with `scopedPurge: true` → declared collections' sealed CEKs purged; the undeclared collection's entries NOT deleted; a Residue notice reports them (count + collection); (c) SCOPED blob arm — declared (`blobFieldsRegistry`) collections' blobs purged; undeclared collections skipped with a residue notice, no per-collection list() scan performed for them (spy the store's list calls); (d) knob is per-vault — two vaults from one noydb instance, one scoped one not, behave independently. Run: (b)-(d) fail, (a) passes (it pins today's behavior).
- [ ] **Step 2: implement** per the architecture above. The sealed-CEK partition is in-memory over the existing vault-wide listing (no new store calls); the blob scoped branch skips undeclared collections' scans entirely (that IS the perf win) and reports the skip. Ceiling discipline: report every vault.ts offset (before/after).
- [ ] **Step 3: run** the new file + the seam map §5 pinning suites + `pnpm check:architecture` + `pnpm --filter @noy-db/hub typecheck`. All green.
- [ ] **Step 4: commit** — `feat(hub): opt-in scopedPurge knob — declaration-gated forget purges with residue reporting (#633)`.

### Task 2: docs + changeset + gauntlet

**Files:**
- Modify: the forget/retention subsystem doc (seam map §4 names the page — likely `docs/subsystems/forget.md` or the retention doc) — document the knob, the candor law (default unconditional BECAUSE declarations under-approximate usage; scoped mode trades completeness for cost and reports what it skips), and the Residue reason strings.
- Create (LOCAL ONLY, not committed): `.changeset/633-scoped-purge.md` — `'@noy-db/hub': minor`.

- [ ] **Step 1:** docs sweep (grep docs/subsystems for `#633`, `unconditional`, `SEALED_CEK` stale claims); update SERVICES.md forget/audit row only if its prose now lies.
- [ ] **Step 2:** author the changeset locally; do NOT git add.
- [ ] **Step 3:** full gauntlet — `pnpm --filter @noy-db/hub test`, typecheck, lint, `pnpm check:architecture`, build + bundle-check. Report ceilings + counts. Grep the diff for attribution/client-name patterns (zero hits).
- [ ] **Step 4: commit** — `docs(hub): scopedPurge forget semantics (#633)`.

## Self-review notes
- The ratified hybrid maps to: default-unconditional pinned by (a); scoping via declarations pinned by (b)/(c); per-vault pinned by (d); candor via Residue reporting.
- vault.ts ceiling: predicate lives service-side; only guard lines in vault.ts, offset-mandated.
