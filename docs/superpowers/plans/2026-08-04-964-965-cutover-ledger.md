# cutover ledger integrity: #964 payloadHash + #965 bare-bump audit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Make schema-cutover migrations survive backup/restore integrity checks (#964) and leave an audit trail for a bare generation bump (#965).

**Architecture:** `_applyCutoverTransform` currently writes per-record `migration` ledger entries with an EMPTY `payloadHash`, so `verifyBackupIntegrity` — which recomputes the ciphertext-domain hash of each record's latest write and compares — fails on any pod that took a data cutover with history on (#964). The fix computes the real `envelopePayloadHash` over the exact ciphertext the cutover persisted, exactly as `put()`/`delete()` and the #941 manifest-sync writer do. Because `collection.ts` sits at its line ceiling (zero slack), we first extract `_applyCutoverTransform` to a sibling module (shrink-first) — which also moves the edited code off the ceilinged surface. Separately, a bare `runCutover` generation bump with no re-declare writes no ledger evidence at all (#965); add one lifecycle audit entry on that path.

**Tech Stack:** TS ESM, `crypto.subtle` only (hub portability), vitest, pnpm. Package: `@noy-db/hub`.

## Global Constraints
- Branch `fix/964-965-cutover-ledger` (off main). Commit per task. **NEVER add Claude/AI attribution.** Grep the diff for any private-client name before each commit.
- Hub portability: no Node built-ins, `crypto.subtle` only (the hash fn already respects this).
- `collection.ts` is at its kernel-surface ceiling (`scripts/check-architecture.mjs` → `'packages/hub/src/kernel/collection.ts': 4329`, actual `wc -l` 4328). Task 1 LOWERS that number after the extraction. Do NOT push collection.ts over its (new) ceiling. `vault.ts`/`noydb.ts` are also at ceilings — do not touch unless the task says so (#965 touches fence-controller.ts, not those).
- Gates: `pnpm --filter @noy-db/hub build && pnpm --filter @noy-db/hub test` + `pnpm --filter @noy-db/hub typecheck` + `pnpm check:architecture` + `pnpm lint`. All green.

## Verified source facts (from recon — see .superpowers/sdd/scratch-964-965-recon.md if present)
- `_applyCutoverTransform` — `packages/hub/src/kernel/collection.ts:2370-2403`. Per live record: decode → `transform` → `nextVersion = _v+1` → `encryptRecord(...)` **inline inside** `this.adapter.put(...)` at ~:2392 (envelope discarded) → cache refresh → ledger append (:2394-2399) with `payloadHash: ''`, `op:'migration'`, `reason:'schema:coordinated-cutover'`, `.catch(()=>{})` best-effort.
- Hash fn: `envelopePayloadHash(envelope): Promise<string>` (`with-commit/history/ledger/hash.ts:34`), exposed as `this.strategies.history.envelopePayloadHash` (no-history binding returns `''`). Call-site precedents: `put()` collection.ts:1786, `delete()` :2562, and #941 manifest-sync `with-shape/manifest/sync.ts:226-234` (its comment explains why the ciphertext-domain hash is mandatory to avoid a false-positive restore tamper failure — the exact bug of #964).
- `verifyBackupIntegrity` — `with-pod/backup.ts:344` recomputes `envelopePayloadHash` per record's latest write and compares.
- `runCutover` — `with-shape/schema-update/fence-controller.ts:100-141`; barrier callback bumps generation via `#setState(nextVersion,'complete'/'normal')` (:134); `#setState` (:149-152) writes fence state + emits an event, NO ledger entry. Vault wiring: `vault.ts:1018 runSchemaCutover` → `_applyCutoverTransform`.
- Ceiling check: `check-architecture.mjs:969` value `4329`, comparison `lines > ceiling` (:1410).
- Tests: `__tests__/schema-update/apply-cutover.test.ts` (1 test, no history/backup). Backup patterns: `__tests__/verifiable-backup.test.ts` (`verifyBackupIntegrity()` :184-336), `__tests__/ledger-hash-sealed.test.ts`. Integration: `__tests__/coordinated-cutover-integration.test.ts`, `coordinated-cutover-multiclient.test.ts`, `schema-update/cutover.test.ts`, `schema-update/fence-controller*.test.ts`. NO test combines cutover + verifyBackupIntegrity today.

---

### Task 1: SHRINK-FIRST — extract `_applyCutoverTransform` to a sibling module (pure refactor)

**Files:** Create `packages/hub/src/kernel/cutover-transform.ts`; modify `packages/hub/src/kernel/collection.ts` (:2370-2403 → a thin delegator); modify `scripts/check-architecture.mjs` (lower the collection.ts ceiling).

**Behavior: NONE changes.** This is a mechanical extraction. `payloadHash: ''` is moved VERBATIM (Task 2 fixes it). All existing cutover tests must stay green with no edits.

- Extract the body of `_applyCutoverTransform` into a free `export async function applyCutoverTransform(ctx: CutoverTransformContext): Promise<void>` in `cutover-transform.ts`. Define `CutoverTransformContext` as an interface of exactly the members the body uses — from recon: `adapter`, `vault`, `name`, `codec`, `ledger`, `strategies`, `keyring`, `vdigFields`, `storeCiphertext`, `tiers`, plus the methods it calls (`resolveRecordCek`, `_onRecordMutated`, and any cache-refresh helper) and the loop inputs (`transform`, the live-record source). Read the actual body first and enumerate every `this.` reference — the interface must cover ALL of them. Bound methods must be passed as arrow wrappers or `.bind(this)` so `this` stays correct.
- `collection.ts`'s `_applyCutoverTransform` becomes a ~3-line delegator: `return applyCutoverTransform({ adapter: this.adapter, /* …all ctx… */, transform, ... })`.
- After extraction, run `wc -l packages/hub/src/kernel/collection.ts` and set the ceiling in `check-architecture.mjs` to that exact number (ratchet DOWN — never leave it higher than actual).
- [ ] **Step 1: extract** — create cutover-transform.ts, convert `this.X`→`ctx.X`, wire the delegator. Do NOT change any logic or the empty payloadHash.
- [ ] **Step 2: green (no test edits)** — `pnpm --filter @noy-db/hub test` (all cutover tests pass unchanged) + `pnpm --filter @noy-db/hub typecheck`. If a private is missing from the ctx, typecheck catches it.
- [ ] **Step 3: ratchet ceiling** — `wc -l` collection.ts, set check-architecture.mjs to that value; `pnpm check:architecture` green.
- [ ] **Step 4: commit** — `refactor(hub): extract applyCutoverTransform to a sibling module (shrink-first, no behavior change)`

---

### Task 2: #964 — real ciphertext payloadHash on cutover migration entries

**Files:** `packages/hub/src/kernel/cutover-transform.ts` (from Task 1); new test `packages/hub/__tests__/cutover-backup-integrity.test.ts`.

**Behavior:** Capture the encrypted envelope that cutover persists into a local const (instead of building it inline in the `adapter.put` call), then set the ledger entry's `payloadHash` to `await ctx.strategies.history.envelopePayloadHash(migEnvelope)` — the same envelope passed to `put`. Mirrors `put()` (collection.ts:1786) and manifest-sync (`sync.ts:233`). With no-history the binding returns `''` (unchanged, harmless).

- [ ] **Step 1: failing test** — `cutover-backup-integrity.test.ts`: create a vault/collection **with history on** (`withHistory()` strategy — check how verifiable-backup.test.ts wires history), put a data record, run a `coordinatedCutover`/`runSchemaCutover` with a transform that changes the record, then `verifyBackupIntegrity()` (or dump→restore) MUST pass. This FAILS today (empty payloadHash → recomputed hash mismatch → BackupCorruptedError/false tamper). Model the backup wiring on `verifiable-backup.test.ts:184-336`. Assert a clean integrity result (and, if practical, assert the migration ledger entry's `payloadHash` is non-empty and equals `envelopePayloadHash` of the stored envelope).
- [ ] **Step 2: run red** — confirm it fails with the integrity/tamper error (not a wiring error).
- [ ] **Step 3: implement** — hoist the envelope const in `applyCutoverTransform`; compute the real hash for the ledger entry.
- [ ] **Step 4: green** — new test passes; full `pnpm --filter @noy-db/hub test` green (no regression); typecheck; `check:architecture` (collection.ts unaffected — edit is in cutover-transform.ts).
- [ ] **Step 5: commit** — `fix(hub): cutover migration entries carry a real ciphertext payloadHash so restore verifies (#964)`

---

### Task 3: #965 — audit a bare runCutover generation bump

**Files:** `packages/hub/src/with-shape/schema-update/fence-controller.ts` (the `runCutover` barrier path); a test in `packages/hub/__tests__/schema-update/` (extend `cutover.test.ts` or a new `bare-cutover-audit.test.ts`).

**Behavior:** After the generation bump in `runCutover`'s barrier callback (post `#setState(...,'complete')`), if a ledger is available, append ONE lifecycle/audit entry recording the bump — `op:'migration'` (or the lifecycle op used elsewhere), `reason: 'schema:generation-bump=<n>'`, `payloadHash:''` (no record ciphertext to hash for a bare bump), best-effort (`.catch(()=>{})` like the per-record path). Confirm how `fence-controller.ts` can reach a ledger (it may need one passed in from `vault.ts:runSchemaCutover` — check the wiring; if the ledger isn't in scope there, thread it minimally or emit via the existing event the vault already ledgers). Keep it minimal; do NOT duplicate per-record entries (those already fire from `applyCutoverTransform` when records+transform exist).

- [ ] **Step 1: failing test** — a `runCutover`/generation bump with NO re-declare and NO records (or an empty transform) leaves exactly one audit ledger entry naming the new generation. Assert the ledger contains it (and that a cutover WITH per-record migrations doesn't double-count — the bump entry is distinct from per-record entries). Read `cutover.test.ts`/`fence-controller*.test.ts` for the fixture style.
- [ ] **Step 2: run red.**
- [ ] **Step 3: implement** the single audit append (thread the ledger if needed).
- [ ] **Step 4: green** + typecheck + `check:architecture`.
- [ ] **Step 5: commit** — `fix(hub): audit a bare schema generation bump in the ledger (#965)`

---

### Task 4: changesets + full gates

**Files:** `.changeset/cutover-payloadhash.md`, `.changeset/bare-cutover-audit.md`.

- [ ] **Step 1: changesets** —
  - `.changeset/cutover-payloadhash.md` (`'@noy-db/hub': patch`, #964): coordinated-cutover migration ledger entries now carry the real ciphertext-domain `payloadHash`, so a pod that took a data cutover with history on passes `verifyBackupIntegrity`/restore instead of failing as tampered.
  - `.changeset/bare-cutover-audit.md` (`'@noy-db/hub': patch`, #965): a bare schema generation bump (`runCutover` with no re-declare) now writes a ledger audit entry recording the new generation.
- [ ] **Step 2: full gates** — `pnpm --filter @noy-db/hub build && test`, `pnpm --filter @noy-db/hub typecheck`, `pnpm check:architecture`, `pnpm lint`. All green.
- [ ] **Step 3: commit** — `docs: changesets for cutover ledger integrity (#964, #965)`

## Out of scope
- Any behavior change to the hash function, the backup format, or the fence state machine beyond the single audit entry.
- #948/#947/#945 (separate branches).
