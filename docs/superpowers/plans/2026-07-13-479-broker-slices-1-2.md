# #479 Credential broker — slices 1+2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship #479 (milestone #24) slices 1+2 per the signed-off spec `docs/superpowers/specs/2026-07-05-credential-broker-spec.md`: the vendor-neutral adapter `credentials` hook (slice 1) and the `@noy-db/hub/broker` service with the enclave proof crypto (slice 2). Slice 3 (sealed-to-instance) stays DEFERRED (spec OQ4 open). noy-db-to adoption happens AFTER the next hub publish (separate repo arc, not this plan).

**Architecture:** The SPEC is the requirements document — every task below names its spec sections and conformance vectors; implementers read the spec sections verbatim. `.superpowers/sdd/broker-seam-map.md` is the CURRENT-TREE ground truth correcting the spec's 2026-07-05 citations; where spec and seam map disagree on locations/idioms, the seam map wins. Key corrections already established:
- **C1/C2 are SHIPPED** (main 00f1c877, v0.3.0-pre.5): `isSecretBearingReservedCollection` (with-party/team/reserved-secret-collections.ts, already exporting `BROKER_COLLECTION = '_broker'`) gates `vault.collection()` and grant-propagation. The broker does NOT ship these guards — it TESTS against them (V11/V17 assert the shipped behavior covers `_broker`).
- **Template correction:** strategy contracts + `NO_*` stubs live at `port/with/<name>-strategy.ts` (the post-#629 convention); the service folder re-exports. Model on `with-party/team`/`session`, NOT the moved classified files.
- **Option site correction:** the strategy option goes in `kernel/types.ts`'s `NoydbOptions` vault-singleton bucket (alongside `shadowStrategy`/`teamStrategy`/`custodyStrategy`) — there is no `NoydbConfig`.
- **/adapter is retired** (pre.10): the spec's "alias golden untouched" lines are obsolete — no alias exists. The `/to` golden's stale `"source"` metadata (spec A6) is still broken and gets fixed here.
- **features.yaml is gone** from this repo (extracted to noy-db-docs): skip the spec's §7 features.yaml instruction; note it as doc-debt for the noy-db-docs sync.

**Tech Stack:** TypeScript ESM, vitest, `crypto.subtle` + `fetch` only (hub-portable, no-crypto-deps), packages/hub + packages/as-aws-s3.

## Global Constraints

- **Ceilings (metric = `split('\n').length`), ALL at exact zero slack:** `kernel/vault.ts` ≤ 3939, `kernel/collection.ts` ≤ 4472, `kernel/noydb.ts` ≤ 2385. The `vault.broker()` accessor MUST be offset in-file. No ceiling bumps.
- **Zero-knowledge law:** passphrase/KEK/DEK/seed never leave the client; the broker sees only `{vaultId, brokerId, proofKey(derived bits, enrol-only), challenge, proof, profile, instancePid?}` (spec §2, V7). Proof crypto lives behind the enclave Contract v1 barrel (`kernel/enclave/broker/proof.ts`); seed lifecycle + fetch + cache live in `with-party/broker/**` and reach crypto ONLY through the barrel (enclave-body-only law). Opaque proof/challenge/credential strings in transit are permitted anywhere.
- **Hygiene:** zero `seedBytes` AND `proofBits` in `try{}finally{}` on every path incl. throws (spec F5, the `recovery.ts` pattern). MAC verification via `crypto.subtle.verify('HMAC', …)` — never Node `timingSafeEqual` (F6). Canonical = `JSON.stringify(['noydb-broker-proof-v1', vaultId, endpointOrigin, brokerId, profile ?? '', instancePid ?? '', challenge, expiresAt])` with `profile` and `endpointOrigin` bound (F1/F4); empty-string `instancePid` forbidden (F7); verbatim `challenge`/`expiresAt` strings MAC'd (F8).
- **Goldens touched DELIBERATELY:** `to-surface.golden.json` (+2 types, + fix stale `source` metadata), enclave Contract v1 barrel golden (+ new fns), kernel-api golden (`vault.broker()`). Each golden edit listed in the task report.
- **Behavior lock:** full existing suite green; TDD per task; no Claude attribution; changeset LOCAL only; conventional commits ending `(#479)`.
- The spec's conformance vectors (V-A0..V-A4, V1..V23, V-KEK, V2b) are the acceptance tests — each task lists ITS vectors; the implementer writes them as named tests citing the vector id in the test name.

---

### Task 1: Slice 1 — the `credentials` adapter seam (hub types + as-aws-s3)

**Spec sections:** §4 (all), §8 Slice 1. **Seam map:** §C.

**Files:**
- Modify: `packages/hub/src/kernel/types.ts` (add `StoreCredentials` + `StoreCredentialSource` — the spec §4 code block verbatim, near the existing `StoreAuth`/`StoreAuthKind`, which stay UNTOUCHED)
- Modify: `packages/hub/src/port/to/index.ts` (re-export the two types — `export type { … } from`, the golden-freezable form per spec H-3)
- Modify: `packages/hub/__tests__/to-surface.golden.json` (+2 types; fix the stale `"source"` metadata to `src/port/to/index.ts` — A6)
- Modify: `packages/as-aws-s3/src/…` (factory options gain `credentials?: StoreCredentialSource` by direct import; thread into the lazily-built S3 client config as a functional provider with `expiration: new Date(expiresAt)` — spec A1/A5 `mapAws` mapping; no `credentials` key ⇒ ambient chain preserved)
- Test: hand-written per-store tests in as-aws-s3's suite + a hub-side type-surface test (spec A2: the adapter-conformance harness has NO credential hook — do not claim it)

**Vectors:** V-A0 (`mapAws` emits `expiration: Date`; missing `expiresAt` → `undefined`), V-A4 (functional provider; no key ⇒ ambient chain), V14 (golden gains exactly the two types; source metadata corrected). V-A1..A3 are `kind:'token'`-path only — NOT in scope (no token store adopts in this repo).

- [ ] Step 1: failing tests (golden mismatch + as-aws-s3 vectors) → Step 2: implement → Step 3: run as-aws-s3 suite + to-surface golden + `pnpm check:architecture` → Step 4: commit `feat(hub,as-aws-s3): StoreCredentials/StoreCredentialSource on /to + credentials refresh hook (#479)`.

### Task 2: Slice 2a — enclave proof crypto (`kernel/enclave/broker/proof.ts`)

**Spec sections:** §2 (protocol steps 2-5), §3 (derivations — the code block verbatim; the six enclave functions with exact signatures), §7 enclave-vs-party boundary. **Seam map:** §B items 6 (HKDF salt/info convention excerpts, barrel + golden).

**Files:**
- Create: `packages/hub/src/kernel/enclave/broker/proof.ts` — `deriveBrokerProofBits`, `deriveBrokerProofKey`, `computeBrokerProof`, `issueChallenge`, `verifyBrokerProof` (+ domain constants `'noydb-broker-proof'` / `'noydb-broker-proof-v1'`), per spec §3 signatures exactly. `verifyBrokerProof` order: (1) `await consumeChallenge(challenge)` burn-and-check-fresh FIRST, short-circuit on not-fresh before any MAC work (F2); (2) recompute canonical + `crypto.subtle.verify` (F6); (3) `now < expiresAt`.
- Modify: `kernel/enclave/index.ts` Contract v1 barrel (+5 fns, ADDITIVE) + its golden (deliberate bump).
- Test: `packages/hub/__tests__/broker/proof.test.ts` (new)

**Vectors (primitive-level):** V1 (binds vaultId), V3 (binds challenge), V4 (binds expiresAt; client clock untrusted), V5 (burn-on-presentation before compare — a spy proves `consumeChallenge` is called before any HMAC work and a burned challenge 401s even with a byte-identical valid proof), V18 (profile-binding: proof for `'read'` fails at `'admin'`), V19 (endpoint-binding: same brokerId, different origin fails), V12 (steady-state `['sign']` key non-extractable — `exportKey` throws), F7 (empty-string instancePid rejected; absent ≠ ''), F8 (reformatted `expiresAt` — `Z` vs `+00:00` — fails; verbatim strings verify), F5 (zeroing: after any op incl. a thrown path, the seed/bits buffers read all-zero — test via injected failing importKey where feasible).

- [ ] Step 1: failing vector tests → Step 2: implement → Step 3: run + barrel golden + `pnpm check:architecture` (enclave-body-only extends to the new identifiers) → Step 4: commit `feat(hub): enclave broker proof primitives — HKDF proof key, challenge mint, burn-first HMAC verify (#479)`.

### Task 3: Slice 2b — `withBroker()` service, seed lifecycle, cache, accessor

**Spec sections:** §2 (enrol/rotate/refresh flows — every I-finding semantics), §3 (`_broker` namespace + lifecycle), §5 (service wiring, client surface, BrokerConfig), §6 (error matrix R-B1..R-B8). **Seam map:** §A (shipped guards — TEST not ship), §B items 5/7/8/9, §D.

**Files:**
- Create: `packages/hub/src/port/with/broker-strategy.ts` — `BrokerStrategy` + `NO_BROKER` throwing stub (post-#629 convention; spec §5 shape, `BrokerCtx = { store, vault, keyring, config }`).
- Create: `packages/hub/src/with-party/broker/{active.ts,index.ts,seed.ts}` — `withBroker(config)` with dynamic-import seam (active.ts); seed lifecycle in seed.ts modeled on `sync-credentials.ts`: `BROKER_SEED_COLLECTION = '_broker'` (import the CONSTANT from reserved-secret-collections.ts — already exists), record id = brokerId, payload `{brokerId, seed, endpoint, createdAt, registered}`, `requireAdminAccess` role gate, **two-phase CAS enrol** (I4/I9: create-if-absent version-guard; `registered: true` only after 2xx; `credentialSource` fail-fast `BrokerEnrolmentError` while unregistered), **KEK-present enrol** (I3/R-B8: intercept the `persistKeyring` kek===null throw → `BrokerEnrolmentError('re-authenticate to enrol')`), **rotate = quiesce-then-swap** (I5: epoch-bump + await in-flight, register-new-first, then overwrite), challenge client + **single-flight per-profile cache** (H-6/I6: in-flight promise cleared-on-reject via try/finally; floored boundary `max(expiresAt − skewMs, now + minCacheMs)`; default skewMs 60_000).
- Modify: `kernel/errors.ts` (+`BrokerNotEnabledError`, `BrokerEnrolmentError`, `BrokerProofError` — additive, follow the idiom).
- Modify: `kernel/types.ts` `NoydbOptions` (+`brokerStrategy?: BrokerStrategy` in the vault-singleton bucket) and the vault wiring (`?? NO_BROKER` default, per the teamStrategy precedent).
- Modify: `kernel/vault.ts` (+`broker(): CredentialBrokerHandle` accessor — the `frame()` idiom per seam map; ceiling offsets MANDATORY, report before/after) + kernel-api golden (deliberate addition).
- Modify: `scripts/check-architecture.mjs` `STRATEGY_GATED_APIS` (+`{ api: /\.broker\s*\(/, option: 'brokerStrategy', factory: 'withBroker' }`).
- Modify: hub `package.json` exports (+`./broker` subpath) + tsup entry; bundle-check: add the broker leak-canary so `NO_BROKER` stays 0-byte when not opted in (spec §5 bundle impact).
- Test: `packages/hub/__tests__/broker/{service.test.ts,seed.test.ts,cache.test.ts}` (new; `fetch` DI via `BrokerConfig.fetch`).

**Vectors:** V2b (role gate), V6 (rotate revokes old — except grace window), V7 (fetch-spy: bodies carry no seed/DEK/passphrase), V8 (single-flight: N concurrent near-expiry calls → exactly one round-trip), V9 (floored refresh boundary), V10 (enrol refused without attestation — reference-host 401 → `BrokerEnrolmentError`), V11 (`vault.collection('_broker')` → reserved error — asserts the SHIPPED guard covers `_broker`), V13 (broker down ⇒ store-level `NetworkError` shape; writes queue — hub-side with a mock token-path store honoring the R-B4 discipline, honestly scoped since the real AWS stores adopt in noy-db-to), V13b (a `BrokerProofError` mid-flush requeues, not drops — same mock-store scope), V17 (grant() sub-admin keyring has NO `_broker` DEK — asserts the SHIPPED exclusion), V20 (enrol-CAS: two concurrent enrols → one seed/DEK), V21 (rotate-quiesce + grace window: in-flight old-key proof still verifies; no `BrokerProofError` surfaced), V22 (cache hygiene: rejected promise cleared; sub-2×skew TTL floored), V23 (partial enrol: 401'd POST leaves `registered !== true` → fail-fast `BrokerEnrolmentError`; later successful enrol mints), V-KEK (DEK-only keyring: enrol → `BrokerEnrolmentError`; use of existing seed succeeds).

- [ ] Step 1: failing vector tests (fetch-DI test host implementing challenge-store burn semantics inline) → Step 2: implement → Step 3: run broker suites + kernel-api golden + bundle-check + `pnpm check:architecture` + full `pnpm --filter @noy-db/hub test` → Step 4: commit `feat(hub): @noy-db/hub/broker — withBroker() passphrase-bound rolling store-auth (#479)`.

### Task 4: docs, reference host, SERVICES.md, changeset, gauntlet

**Spec sections:** §7 (governance; reference-host obligations — KMS-wrap MANDATED, burn-on-presentation, rate-limit SHOULD, rotate grace window), §1 candor table (the docs ship the DOES-NOT-defeat column honestly), §9 non-goals.

**Files:**
- Create: `docs/subsystems/broker.md` (in-repo convention; the noy-db-docs page is later doc-sync debt) — service doc with the candor table, the enrol/challenge/mint/rotate flows, the ~100-line reference Lambda/STS host embedding `issueChallenge`/`verifyBrokerProof` + the four host obligations, and the `BrokerConfig` JSDoc-mirrored guidance (globally-unique brokerId, F4).
- Modify: `SERVICES.md` Cluster G row (per spec §7 text; ~500 LOC off-bundle).
- Create (LOCAL ONLY): `.changeset/479-broker.md` — `'@noy-db/hub': minor` + `'@noy-db/as-aws-s3': minor` (slices 1+2 together per spec §7 changeset plan).
- Note in report: features.yaml instruction SKIPPED (file extracted to noy-db-docs — doc-debt note), noy-db-to adoption + peer-floor bump (spec A3 manual gate) queued post-publish.

- [ ] Step 1: docs (examples MUST be lifted from the shipped tests — the phase-B "docs-from-shipped-tests" mandate) → Step 2: changeset local → Step 3: full gauntlet (hub test/typecheck/lint, check:architecture, build + bundle-check incl. the new broker canary; as-aws-s3 test) + attribution/client-name grep → Step 4: commit `docs(hub): broker service page + SERVICES.md row (#479)`.

## Self-review notes
- Spec coverage: slice-1 §4/§8-S1 → T1; §2/§3 crypto → T2; §2/§3/§5/§6 service → T3; §7 → T4. All 20 slice-1/2 vectors assigned (V15/V16/V2-slice-3 arm deferred with slice 3; V-A1..A3 out with token stores).
- C1/C2: tested (V11/V17), not shipped — the seam map proved they landed in pre.5 with `_broker` pre-reserved.
- Ceilings: only T3 touches vault.ts (+accessor) — offsets mandated; T1/T2/T4 live outside ceiling files.
