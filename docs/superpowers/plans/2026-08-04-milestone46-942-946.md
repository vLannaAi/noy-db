# Milestone #46 — implement #942 + #946 (scheduled resume brief)

> This is a self-contained brief for a cloud agent resuming milestone #46 with **zero prior context**. Read it fully, then execute. Everything you need is in this repo and in GitHub issues.

## Task

Implement TWO GitHub issues from milestone **#46** (`pod-extensions: manifest-set foundations`), each as its **own branch and its own PR to `main`**:

- **#942** — pod-header field extensions (layer L2)
- **#946** — schema-identifiers: stable field IDs + generation↔content-hash binding (layer L6)

They are **independent** of each other. Do #942 first, fully land its PR, then #946 — so partial progress is preserved if you run out of time.

## Orient first (in this order, before writing any code)

1. `gh issue view 950` — the milestone tracking issue: the 9-issue set, the suggested order, and the **Definition of Done**. DoD = all nine ship in **one `@next` batch pre-release LATER**. Therefore **you do NOT release or publish anything** — no `npm publish`, no `changeset publish`, no GitHub release. You only open PRs to `main`.
2. `gh issue view 942` and `gh issue view 946` — the full request + **acceptance criteria** for each. These ARE your requirements. Every acceptance checkbox must be satisfied and verified by a test.
3. Read `docs/superpowers/plans/2026-08-03-pod-signature.md` — the plan for the just-merged #943. **Use it as your template** for rigor: task decomposition, test-first (TDD) steps, the gate list, and golden/ceiling handling. Also read `docs/subsystems/pod-signature.md`, the root `CLAUDE.md`, and `packages/hub/CLAUDE.md` for conventions.

## Key cross-issue facts

- **#943 (pod signature) is ALREADY MERGED to main.** `packages/hub/src/with-pod/format.ts` now has: header fields `sig`/`keyId`/`sigAlg`; `formatVersion` accepting `1` or `2` (const `NOYDB_BUNDLE_FORMAT_VERSION_SIGNED = 2`); an all-or-nothing sig-tuple validator; and the `ALLOWED_HEADER_KEYS` set.
- **#942 extends the SAME four spots additively**: `ALLOWED_HEADER_KEYS`, the `NoydbPodHeader` interface, `validateBundleHeader`, and `encodeBundleHeader`'s explicit key-order build. Its new keys (see the issue — e.g. engine range, unlock methods, app flag, species hint, pointer mode) join the header the same way sig fields did.
- **The pod signature covers "whole header minus sig"**, so #942's new fields join the signature automatically. **Add a test** proving a signed pod that carries #942's new header fields still verifies via `verifyPodHeader` — this is the load-bearing regression guard for the #942↔#943 seam.
- Both issues feed #941 (manifest engine) — **#941 is out of scope** for this run.

## Process (mirror the #943 loop)

For **each** issue, independently:

1. **Plan.** Write a short implementation plan to `docs/superpowers/plans/2026-08-04-<issue>-<slug>.md` and commit it. Decompose into small test-first tasks, exactly as the #943 plan does.
2. **Implement test-first.** For every behavior: write the failing test, run it red, implement minimally, run it green. Real tests, no tautologies. Hub tests live in `packages/hub/__tests__/`.
3. **Self-review** the diff against the issue's acceptance criteria before opening the PR — check each checkbox is actually met and tested, YAGNI, no scope creep, house style matches neighbors.
4. **Full gates — all must pass** (run from repo root):
   - `pnpm --filter @noy-db/hub build`
   - `pnpm --filter @noy-db/hub check:types` — **dist-based type-reachability; run AFTER build.** NOT in the default lint/test set and it has bitten this repo. If you add an export whose signature names a type, export that type from the **same entry** or it fails. If it reports FIXED gaps, ratchet: `cd packages/hub && BASELINE_UPDATE=1 node scripts/check-type-reachability.mjs` and commit the baseline.
   - `pnpm --filter @noy-db/hub test`
   - `pnpm check:architecture`
   - `pnpm lint`
   - `pnpm typecheck`
5. **Changeset.** Author a local `.changeset/<slug>.md` (`'@noy-db/hub': minor`) describing the change (`.changeset/` is gitignored — create the file anyway; repo convention). Do NOT bump versions or publish.
6. **PR.** Open a PR to `main` via `gh pr create` (title `feat(hub): ... (#<issue>)`). Body: what it does, acceptance criteria met, gate status. If `gh pr create` fails, push the branch and report the branch name.

## Guardrails / gotchas (from the #943 arc)

- **NEVER add Claude/AI attribution** to commits, PRs, changelogs, or changesets (family rule — grep your diff before committing).
- **Never publish.** No release commands of any kind.
- **Goldens freeze exports.** Adding an export from `@noy-db/hub` or `@noy-db/hub/pod` trips `pod-surface-golden.test.ts` / `root-barrel-surface-golden.test.ts` — update the golden JSON **and** the `_FrozenTypes` tuple / `import type` list in the same commit. **Never touch `cargo-surface.golden.json`** — if it trips you exported into the wrong place.
- **`format.ts` changes must stay back-compatible**: a pre-#942 header (no new fields) must still validate and round-trip; unsigned v1 pods still read/write; the all-or-nothing sig-tuple invariant must survive. Follow the format's own law: new header keys require the validator + encoder to handle them, and (if the issue calls for it) a version consideration — check what #942's acceptance criteria say about versioning.
- **Kernel line ceilings are tight** (`scripts/check-architecture.mjs` KERNEL_SURFACE_BUDGET: `collection.ts`/`vault.ts`/`noydb.ts`). If you must add to one, measure with `node -e "console.log(require('fs').readFileSync('<file>','utf8').split('\n').length)"` and shrink-first if over. #942/#946 should mostly avoid these files (format/identifier work lives in `with-pod` / schema code).
- **kernel → `with-*` service imports** need the sanctioned dynamic-`import()` gate (a static import, even type-only, trips port-layering per-specifier). If #946's schema-identifier work touches the kernel schema layer and needs a service type, use the dynamic-import escape hatch (see how `vault.ts` does it).
- **TDD real crypto**: hub tests use real `crypto.subtle` (no mocks). Signed-pod tests derive fresh; keep any generous timeouts.

## Definition of done for this run

Two PRs open against `main` (#942 and #946), each with its acceptance criteria met and verified, all gates green, a local changeset authored, no releases. Report both PR URLs (or branch names if PR creation failed). If you complete only one, that PR still stands — leave the other's branch pushed with a note on what remains.
