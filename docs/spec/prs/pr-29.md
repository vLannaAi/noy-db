# Pull Request #29 — docs: v0.3 sweep — Pinia/Nuxt happy path + fix @noydb/* scope (closes #17)

- **State:** MERGED
- **Author:** @vLannaAi
- **Created:** 2026-04-06
- **Merged:** 2026-04-06

- **Branch:** `feat/v0.3/docs-sweep` → `v0.3-dev`
- **Merge commit:** [`458a4f944b`](../../../../../commit/458a4f944b5117bb00efa4ca39e969941da8c467)
- **Labels:** _(none)_

---

## Summary

Comprehensive docs update for v0.3, plus a real bug fix: every doc was using the **wrong package scope** \`@noydb/*\` (a pre-publish placeholder). Replaced with the published \`@noy-db/*\` scope across the docs set, the README, and the architecture SVG — **80+ instances corrected**. Anyone copy-pasting from the docs would have hit \`E404\` from npm.

The v0.3 docs now lead with the \`@noy-db/nuxt\` + \`@noy-db/pinia\` happy path everywhere. The low-level \`createNoydb()\` walkthrough is preserved as a "Going lower" appendix.

## Changes

- **\`docs/getting-started.md\`** — full rewrite. Two install paths (Nuxt 4, plain Vue 3). Pinia store as the first example. v0.3 features (query DSL, indexes, pagination, lazy hydration) get one section each. \`createNoydb()\` is now an appendix.
- **\`docs/end-user-features.md\`** — added a "Developer reference — v0.3 features" section under the existing consumer-facing pitch. Covers Pinia (both paths), Nuxt module, query DSL, indexes, listPage + scan, lazy + LRU. Each section has a runnable snippet.
- **\`docs/architecture.md\`** — flagged "Memory-first" as opt-in vs lazy. Added a **Caching and lazy hydration** subsection with an LRU flow diagram, and a **Pinia layering** subsection with a flow diagram showing how the Pinia store sits on top of \`Collection\` without weakening the encryption boundary.
- **\`docs/adapters.md\`** — added an **Optional capabilities (v0.3+)** section documenting the \`listPage\` capability flag pattern, with a per-adapter cursor format table.
- **\`docs/deployment-profiles.md\`** — added profiles **7a (Nuxt 4 + browser + Dynamo sync)** and **7b (Nuxt 4 + file USB)**, plus rows in the quick-selection table.
- **\`README.md\`** — replaced the Quick Start with the two-minute Pinia story. Added \`@noy-db/pinia\` and \`@noy-db/nuxt\` to the zero-deps table. Updated the Status table from "Pre-release / Planned" placeholders to actual shipped versions.
- **\`docs/assets/architecture.svg\`** — fixed 5 stale \`@noydb/*\` references in the rendered diagram.
- **\`ROADMAP.md\`** — left as-is. Marking v0.3 as shipped happens in the release PR (#18) per issue #17 acceptance criteria.

## Acceptance criteria from #17

- [x] Every file in scope updated with v0.3 examples
- [x] Privacy guard CI passes (ran locally — clean)
- [x] All examples use generic names (no client firm references)
- [x] Cross-links between docs in place
- [x] No new files created — all edits in-place
- [ ] Markdown link check / snippet type-check — leaving to CI
- [ ] Changeset — none needed (docs-only, no package changes)
- [ ] Mark v0.3 shipped in ROADMAP — deferred to release PR #18

## Test plan

- [ ] CI green on \`feat/v0.3/docs-sweep\`
- [ ] Visual review of rendered Markdown on the GitHub PR diff
- [ ] Spot-check that \`docs/getting-started.md\` Path 1 matches what \`playground/nuxt/\` actually does
- [ ] Confirm \`grep -rn '@noydb/' docs README.md ROADMAP.md\` returns nothing

🤖 Generated with [Claude Code](https://claude.com/claude-code)
