# Pull Request #77 — docs(roadmap): @noy-db/decrypt-* package family + plaintext export policy

- **State:** MERGED
- **Author:** @vLannaAi
- **Created:** 2026-04-07
- **Merged:** 2026-04-07

- **Branch:** `chore/roadmap-decrypt-package-family` → `main`
- **Merge commit:** [`de2e88eff8`](../../../../../commit/de2e88eff8f61401a9ef347e8eb3a1f39772d130)
- **Labels:** _(none)_

---

## Summary

- Adds the v0.5 `exportStream()` + `exportJSON()` core primitive (#72) bullet to the v0.5 release section
- New section **Plaintext export packages — `@noy-db/decrypt-*`** documenting:
  - Why JSON lives in core (zero-dep, universal default) and every other format lives in its own package
  - The deliberate `decrypt-{format}` naming and the rationale (the word "decrypt" in the import statement is the consent step)
  - The package list with risk profiles and target releases (`csv` + `xml` zero-dep v0.6; `xlsx` peer-dep v0.7, ships last)
  - The mandatory README warning block format (also lives in JSDoc + npm description)
  - The explicit no-MySQL-DDL position with the userland answer (`mysqlimport` already does this)

Spawned from discussion vLannaAi/noy-db#70 — closes the maintainer-position ask so the discussion can be closed and the format-package follow-up issues can be opened from a documented baseline.

## Test plan

- [x] `./scripts/check-privacy.sh` — clean
- [x] No code changes; docs-only PR

🤖 Generated with [Claude Code](https://claude.com/claude-code)
