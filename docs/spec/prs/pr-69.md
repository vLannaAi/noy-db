# Pull Request #69 — docs: CLI reference + augment mode + AI reference update

- **State:** MERGED
- **Author:** @vLannaAi
- **Created:** 2026-04-07
- **Merged:** 2026-04-07

- **Branch:** `docs/cli-reference-augment-mode` → `main`
- **Merge commit:** [`8252394873`](../../../../../commit/82523948734a105b28eecf322b200ec0c084b756)
- **Labels:** _(none)_

---

## Summary

Three user-facing docs were stale after this session's feature work:

1. **\`packages/create-noy-db/README.md\`** — only documented 2 of 5 subcommands (the v0.3.1 \`add\` + \`verify\`), and had no mention of augment mode or any of the new flags.
2. **\`docs/getting-started.md\`** — no \"add noy-db to my existing Nuxt app\" path.
3. **\`docs/noydb-for-ai.md\`** — no CLI subcommand reference, no mention of \`Noydb.rotate()\`.

This PR fills the gaps.

## Changes

### \`packages/create-noy-db/README.md\` — full rewrite (102 → 266 lines)

New structure:

- **Two-bins explainer** up front
- **\`create @noy-db\` — the wizard** with subsections for:
  - Fresh mode (original v0.3.1 behavior)
  - Augment mode (new in v0.5 via magicast) — includes the safe-behavior list: idempotent, preserves custom config, preserves unrelated keys, rejects opaque shapes
  - \`--dry-run\` documented
  - \`--force-fresh\` documented
  - Full flag table
- **\`noy-db\` — the CLI tool** with one subsection per command:
  - \`add <collection>\`
  - \`add user <id> <role>\` — including the role + permissions table
  - \`verify\`
  - \`rotate\` — with use cases (post-breach, scheduled rotation)
  - \`backup <target>\` — with supported URI schemes and parent-dir creation behavior
- **Security invariants** (5 bullets on passphrase handling)
- **\"Deferred to a future release\"** — updated. The v0.3.1-era entries (Thai i18n, magicast, CLI subcommands) were misleading since we shipped three of them. Replaced with the real current open items (non-Nuxt templates, \`seed\`, S3 backups, \`restore\`).

### \`docs/getting-started.md\` — augment section

New **\"Augmenting an existing Nuxt 4 project\"** section between \"Fastest path\" and \"Path 1 — Manual install\". Covers auto-detection, the three invocations (interactive / dry-run / --yes), safe behaviors, post-wizard install command, and the \`--force-fresh\` escape hatch for monorepos.

Also expanded the existing \"Adding collections to an existing project\" subsection to mention the new \`add user\` / \`rotate\` / \`backup\` commands with real invocation examples.

### \`docs/noydb-for-ai.md\` — CLI reference + rotate method

New top-level **\"CLI Reference\"** section with shell snippets for every subcommand, showing required flags and what prompts fire. Security invariants documented inline. Dependency-injection pattern shown for programmatic use from test code (which the subcommand modules explicitly support via injected \`readPassphrase\` / \`createDb\` / \`buildAdapter\` deps).

New **\"Noydb.rotate() core API\"** subsection covering the distinction between \`rotate()\` (just rotate keys) and \`revoke({ rotateKeys: true })\` (revoke + rotate), plus the in-memory keyring refresh behavior and three use cases.

## Verified

- Privacy guard: clean
- Turbo lint + typecheck: 28/28 tasks green (no code changes in this PR)
- Line counts: 266 / 467 / 737 (reasonable)

## Test plan

- [ ] CI green
- [ ] Reviewer reads each of the three updated files and confirms the content matches intent
- [ ] Spot-check: the \"augment mode\" section in getting-started.md should read naturally alongside the existing fresh-project flow

No feature changes, no code changes. Pure docs.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
