# Pull Request #52 — docs(v0.4): sweep — schema, ledger, deltas, refs, verifiable backups

- **State:** MERGED
- **Author:** @vLannaAi
- **Created:** 2026-04-07
- **Merged:** 2026-04-07

- **Branch:** `feat/v0.4/docs-sweep` → `v0.4-dev`
- **Merge commit:** [`005f361a72`](../../../../../commit/005f361a723fb20a313bef16644b2918be03f3c3)
- **Labels:** _(none)_

---

## Summary

v0.4 docs sweep. Mirrors the v0.3 sweep pattern (#29): one runnable example per feature in \`end-user-features.md\`, a new architecture section for the ledger, a v0.4 highlights block in \`getting-started.md\`, and the README status table flipped to reflect v0.4-shipped.

## Changes

- **\`docs/guides/end-user-features.md\`** — new "v0.4 features — integrity & trust" section with five runnable examples: schema validation (Zod), hash-chained ledger, delta history with reconstruct, FK refs with all three modes, verifiable backups with the attack→detection table.

- **\`docs/reference/architecture.md\`** — new "Hash-chained ledger" section before "Adapter interface":
  - Chain structure with mermaid flowchart
  - Why we hash ciphertext (preserves zero-knowledge)
  - Verify algorithm with mermaid sequence diagram
  - Delta history walk diagram (current state → reverse patches → historical version)
  - Single-writer concurrency caveat

  Threat model table picks up three new v0.4 entries (backup tampering, bad data on put, orphaned refs). Implementation history extended through v0.3.1 and v0.4.

- **\`docs/guides/getting-started.md\`** — "v0.4 feature highlights" section after the v0.3 highlights. Short examples for schema, ledger.verify(), dump/load, and ref() — pointers to \`end-user-features.md\` for the full reference.

- **\`README.md\`** — status table flipped: v0.3, v0.3.1, v0.4 all marked shipped. v0.5+ row added for what's next.

## Out of scope

- **\`ROADMAP.md\` not touched** — that's a release-PR concern. Marking v0.4 as ✅ shipped happens in the next PR alongside the version bumps.
- **No new doc files** — sweep stays in-place per the v0.3 precedent.
- **No noydb-for-ai.md update** — separate concern, lower priority.

## Validation

- Privacy guard: clean
- Full monorepo turbo pipeline: 51/51 tasks green
- All 654 monorepo tests still pass

## Test plan

- [ ] CI green on \`feat/v0.4/docs-sweep\`
- [ ] After merge, the next PR is the release: bump versions, mark v0.4 shipped in ROADMAP, then create the GitHub Release.

Part of #41.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
