# Pull Request #161 — feat(hub): v0.12 storage structure — blob store, syncPolicy, multi-backend

- **State:** MERGED
- **Author:** @vLannaAi
- **Created:** 2026-04-10
- **Merged:** 2026-04-10

- **Branch:** `feat/v0.12-storage-structure` → `main`
- **Merge commit:** [`22a65a9a81`](../../../../../commit/22a65a9a8104944c9beddd0017c35f941f221b37)
- **Labels:** _(none)_

---

## Summary

Implements the four v0.12 milestone issues — the internal hub changes that all future store packages and topologies depend on.

- **#105 Encrypted binary blob store** — `BlobSet` API with HMAC-SHA-256 keyed eTags, AAD chunk integrity, refCount with CAS retry, selective versioning (publish/getVersion), MIME auto-detection (55 rules), `maxBlobBytes` capability
- **#103 NoydbBundleStore interface** — `kind: 'bundle'` discriminant, OCC via version tokens, `wrapBundleStore` with `autoFlush`/`batch`/`flush` modes, LWW conflict merge
- **#101 syncPolicy scheduling** — `SyncPolicy` type (4 push modes, 3 pull modes), `SyncScheduler` with `minIntervalMs` floor, `onUnload` hooks, `INDEXED_STORE_POLICY`/`BUNDLE_STORE_POLICY` defaults
- **#158 Multi-backend topology** — `SyncTarget[]` with `role` semantics (`sync-peer`/`backup`/`archive`), write fanout, `sync:backup-error` event, per-target policy, backward-compatible `NoydbOptions.sync`

### Files changed (15)

| Category | Files |
|----------|-------|
| New source | `blob-set.ts`, `mime-magic.ts`, `sync-policy.ts`, `attachments.ts` (legacy), `bundle-store.ts` |
| Modified source | `crypto.ts`, `types.ts`, `errors.ts`, `collection.ts`, `noydb.ts`, `sync.ts`, `index.ts` |
| New tests | `blob-set.test.ts` (27 tests), `attachments.test.ts` (11 tests, updated) |
| Design | `docs/v0.12-blob-design.md` (revision 2) |

### Design doc

`docs/v0.12-blob-design.md` — comprehensive design covering all use cases, storage layout, crypto invariants, known trade-offs.

## Test plan

- [x] 810 tests passing (`npx vitest run packages/hub`)
- [ ] Verify MIME detection for all 48 format families
- [ ] Manual test: blob round-trip with DynamoDB (256KB chunk limit)
- [ ] Manual test: bundle store round-trip with Google Drive mock
- [ ] Verify `syncPolicy` debounce coalescing with timer mocks
- [ ] Verify multi-target fanout: primary failure blocks, backup failure emits event

Closes #103, #105, #101, #158

🤖 Generated with [Claude Code](https://claude.com/claude-code)
