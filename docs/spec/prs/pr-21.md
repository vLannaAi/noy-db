# Pull Request #21 — ci: trigger on vX.Y-dev branches and stacked feat/* PRs

- **State:** MERGED
- **Author:** @vLannaAi
- **Created:** 2026-04-06
- **Merged:** 2026-04-06

- **Branch:** `ci/v0.3-dev-trigger` → `main`
- **Merge commit:** [`40d6e6e5c4`](../../../../../commit/40d6e6e5c41615cbc8dca2b3b297fe2988b46215)
- **Labels:** _(none)_

---

## Summary

Updates the CI workflow trigger so v0.3 work actually runs through the test suite. The previous trigger only fired on main and PRs against main — PRs against \`v0.3-dev\` and stacked PRs against \`feat/v0.3/*\` had **zero CI coverage**, which I discovered when opening PR #19 and PR #20 and watching no checks appear.

## Changes

\`.github/workflows/ci.yml\` now triggers on:

- \`push\` to \`main\` or any \`vX.Y-dev\` branch
- \`pull_request\` targeting \`main\`, any \`vX.Y-dev\` branch, or any \`feat/**\` branch (so stacked PRs work)

## Why this matters now

Two PRs are currently stuck without CI:

- #19 — \`feat(core): reactive query DSL\` (target: \`v0.3-dev\`)
- #20 — \`feat(pinia): defineNoydbStore + Vue/Pinia playground\` (target: \`feat/v0.3/query-dsl\`, stacked on #19)

Once this PR merges, both will fire CI on their next push.

## Test plan

- [x] YAML parses (verified by GitHub Actions linter on push)
- [x] No code changes — workflow file only
- [ ] CI runs on this PR (the existing trigger fires because it targets \`main\`)

## Notes

The pattern \`v[0-9]+.[0-9]+-dev\` matches \`v0.3-dev\`, \`v0.4-dev\`, \`v1.0-dev\`, etc. — works for any future release branch with no further updates.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
