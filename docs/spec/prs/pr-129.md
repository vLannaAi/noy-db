# Pull Request #129 — feat(core): dictKey/DictionaryHandle + i18nText schema types (v0.8 #81 #82)

- **State:** CLOSED
- **Author:** @vLannaAi
- **Created:** 2026-04-09

- **Closed without merge:** 2026-04-09
- **Branch:** `feat/v0.8/i18n-dict-81-82` → `main`

- **Labels:** _(none)_

---

## Summary

- **#81** — `dictKey(name, keys?)` descriptor + `DictionaryHandle` CRUD for `_dict_*` reserved encrypted collections; `rename()` cascades key rewrites across all referencing collections
- **#82** — `i18nText({ languages, required })` descriptor for per-record multi-language prose; `validateI18nTextValue` enforces required translations on `put()`; `resolveI18nText` / `applyI18nLocale` collapse locale maps post-decryption (primary locale, fallback chain, `'any'` wildcard, `'raw'` pass-through)
- `LocaleReadOptions` for per-call `{ locale, fallback }` on `get()` / `list()`; compartment-level default via `setLocale()` / `getLocale()`
- 5 new error classes: `ReservedCollectionNameError`, `DictKeyMissingError`, `DictKeyInUseError`, `MissingTranslationError`, `LocaleNotSpecifiedError`
- Minor changeset for `@noy-db/core`

## Test plan

- [x] 22 new tests in `dictionary.test.ts` — CRUD, rename cascade, reserved name rejection, ACL (PermissionDeniedError), per-call and compartment-default locale
- [x] 25 new tests in `i18n.test.ts` — `resolveI18nText` utility, `validateI18nTextValue` (all/any/string[] modes), Collection integration (put/get/list with locale, raw mode, fallback chain, error paths)
- [x] 696/696 tests passing
- [x] Lint clean (`eslint src/`)
- [x] TypeScript clean (`tsc --noEmit`)

Closes #81, closes #82

🤖 Generated with [Claude Code](https://claude.com/claude-code)
