# Pull Request #79 — docs(roadmap): rescope v0.5, add v0.6 query-DSL, slip identity to v0.7

- **State:** MERGED
- **Author:** @vLannaAi
- **Created:** 2026-04-07
- **Merged:** 2026-04-07

- **Branch:** `chore/roadmap-v05-rescope` → `main`
- **Merge commit:** [`2e6016933d`](../../../../../commit/2e6016933da0039209353f02305b6bf19890b290)
- **Labels:** _(none)_

---

## Summary

Restructures the v0.5+ release tree to match the new milestone layout.

### v0.5 — retheme from "Identity & sessions" to "Core enhancements + scaffolder polish"

The wizard work is already shipped (#36/#37/#38/#40). The release adds three small, orthogonal core enhancements that were ready sooner than the identity epic:

- **#72** — `exportStream()` + `exportJSON()` core primitive
- **#63** — Cross-compartment `queryAcross` + `listAccessibleCompartments`
- **#62** — Admin-grants-admin bounded delegation with cascade revoke
- **#57** — SVG infographics refresh

These three are independent, low-risk, and bring real consumer value without waiting on the identity/sessions design pass.

### v0.6 — new "Query DSL completion" milestone

Bundles the joins epic with aggregations v1:
- **#73** — Eager single-FK join planner (foundation)
- **#74** — `.join().live()` merged-stream subscription (blocked by #73)
- **#75** — Multi-FK chaining (blocked by #73)
- Aggregations v1 (built-in reducers + groupBy + scan().aggregate(), spawned from discussion #65)

Both features extend the same `.query()` builder and want to land in the same release so the docs cover them together. **#76 streaming joins stays unmilestoned** as a v0.7+ candidate per the original framing — graduates when a real consumer hits the row ceiling.

### v0.7 — Identity & sessions (slipped from v0.5)

Same epic content (session tokens, OIDC bridge, magic links, hardware-key keyrings), one slot later. Nothing dropped.

### Downstream renumbering

- v0.6 sync v2 → **v0.8**
- v0.7 dev experience → **v0.9**
- v0.8 adapter expansion → **v0.10**
- v0.9 framework integrations → **v0.11**
- v1.0 stability + LTS → unchanged

### Other updates

- Status paragraph rewritten to match the new v0.5 theme
- Mermaid gantt updated with new release ordering
- Concerns→releases table updated with the three new v0.5 concerns and bumped v0.7/v0.8 mappings
- `@noy-db/decrypt-{csv,xml,xlsx}` target column updated: csv/xml "post-v0.5, opportunistic" (small, orthogonal, ships between themed releases), xlsx "v0.8+" (after warning workflow is well-rehearsed)
- Roadmap version bumped to v3.2

## Test plan

- [x] `./scripts/check-privacy.sh` — clean
- [x] No code changes; docs-only PR
- [x] Cross-checked release table, gantt chart, and concerns table for version-number consistency

🤖 Generated with [Claude Code](https://claude.com/claude-code)
