# Pull Request #22 — feat(pinia): defineNoydbStore + Vue/Pinia playground

- **State:** MERGED
- **Author:** @vLannaAi
- **Created:** 2026-04-06
- **Merged:** 2026-04-06

- **Branch:** `feat/v0.3/pinia-define-store` → `v0.3-dev`
- **Merge commit:** [`feb1ef5430`](../../../../../commit/feb1ef5430b9c7c15a09c75842394d778597d39b)
- **Labels:** _(none)_

---

## Summary

Adds the \`@noy-db/pinia\` package and a Vue 3 + Vite + Pinia reference playground demonstrating the v0.3 one-line greenfield path.

(Re-opened against \`v0.3-dev\` after the original PR #20 was auto-closed when its stacked base \`feat/v0.3/query-dsl\` got squash-merged into v0.3-dev. Same code, same tests, same scope.)

\`\`\`ts
// stores/invoices.ts
import { defineNoydbStore } from '@noy-db/pinia';

export const useInvoices = defineNoydbStore<Invoice>('invoices', {
  compartment: 'C101',
});
\`\`\`

## Closes

Closes #10
Part of #6
Replaces #20 (auto-closed by stacked-PR base deletion)

## Acceptance criteria touched

From #10:

- [x] **Implementation:** \`packages/pinia/src/index.ts\` exports \`defineNoydbStore\` (ESM 2.61 KB / CJS 3.77 KB)
- [x] **Unit tests:** 18 \`it()\` blocks (acceptance criterion was ≥14)
  - greenfield instantiation against @noy-db/memory
  - items reactivity on add/update/remove
  - byId, count reactivity
  - \`\$ready\` resolves once per instance
  - schema validation throws on invalid input
  - persistence round-trip across store re-creation
  - multi-store isolation
  - \`storeToRefs\` returns reactive refs
  - error when no Noydb instance is bound
  - explicit \`noydb:\` option works without global binding
  - \`prefetch: false\` defers hydration
  - \`query()\` returns chainable Query<T>
  - \`query()\` before \`\$ready\` throws when prefetch is false
  - \`refresh()\` re-hydrates after external mutation
  - \`collection\` option distinct from store id
- [x] **Integration tests:** every test runs the real \`createNoydb()\` pipeline
- [x] **Docs:** \`packages/pinia/README.md\`
- [x] **Changeset:** new package release at \`0.3.0\`
- [x] **Bundle:** ESM 2.61 KB (well under the 8 KB budget)
- [x] **Playground:** \`playground/pinia/\` Vue 3 + Vite + Pinia demo with chainable query DSL filter UI
- [ ] **Type tests** (\`expect-type\`) — deferred to #11 (augmentation plugin)
- [ ] **\$ledger surface** — deferred to v0.4 ledger work (#13)
- [ ] **\`cache.maxRecords\` LRU** — deferred to #15
- [ ] **SSR test** — will land with the Nuxt module #8

## Test plan

- [x] \`pnpm turbo lint typecheck test build\` clean (42/42 tasks green, force run)
- [x] Test totals: 316 passing (was 233 on v0.2 baseline; +65 from query DSL, +18 from pinia)
- [x] Playground builds: \`pnpm --filter @noy-db/playground-pinia exec vite build\` → 38.7 KB gz
- [x] Privacy guard clean
- [x] Manual: ran the playground locally, verified add/seed/filter/delete flow, inspected IndexedDB ciphertext

## Security checklist

- [x] No new runtime crypto dependencies
- [x] No plaintext leaves the crypto layer
- [x] No keys persisted to disk or sent over the network
- [x] No \`_iv\` reuse
- [x] Adapter receives only encrypted envelopes (proven by integration tests)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
